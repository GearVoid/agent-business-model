# Optional but recommended for OpenAlex polite pool / higher rate limits.
OPENALEX_MAILTO=you@example.com

# Optional webhook transport for scripts/deliver.py --transport webhook.
# DELIVERY_WEBHOOK=https://example.com/perovskite-scout-delivery
